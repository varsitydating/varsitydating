import 'package:flutter/material.dart';

class SubscriptionPaymentsScreen extends StatelessWidget {
  const SubscriptionPaymentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Subscription & Payments'),
      ),
      body: Center(
        child: Text('Subscription & Payments Screen'),
      ),
    );
  }
}
